export type DateOptions = {
  startDate: Date,
  endDate: Date
}
export type GetMindfulSession = {
  startDate: string,
  endDate: string,
  getTotalCount?: boolean
}
export type GetStepCount = {
  startDate: string,
  endDate: string,
  getTotalCount?: boolean
}