import * as React from 'react';
import { _Text } from './AppStyledComponents';

const PlaceholderComponent = ({ label }) => <_Text style={{ textAlign: 'center' }}>{label}</_Text>
export default PlaceholderComponent